import React from 'react';
import ReactDOM from 'react-dom';

import api from 'frontend/projeto/src/service/api';

import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
